<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="ProfileSaveChangesButton" TargetType="{x:Type Button}">

          <Setter Property="Background" Value="Transparent"></Setter>
        <Setter Property="Height" Value="25"></Setter>
        <Setter Property="Width" Value="22"></Setter>
        <Setter Property="Template" >
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Ellipse 
                        HorizontalAlignment="Center"
                        Width="{TemplateBinding Width}"       
                        Height="{TemplateBinding Height}"
                    >
                        <Ellipse.Fill >

                            <VisualBrush  >
                                <VisualBrush.Visual >
                                    <Grid Background="Transparent" >
                                        <Image 

                                            Stretch="UniformToFill"
                                            Source="../../Images/OkButton.png"></Image>
                                    </Grid>
                                </VisualBrush.Visual>
                            </VisualBrush>
                        </Ellipse.Fill>
                    </Ellipse>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Width" Value="33"></Setter>
                <Setter Property="Height" Value="29"></Setter>
            </Trigger>

        </Style.Triggers>
    </Style>
    <Style x:Key="ProfileCancelChangesButton" TargetType="{x:Type Button}">

        <Setter Property="Background" Value="Transparent"></Setter>
        <Setter Property="Height" Value="25"></Setter>
        <Setter Property="Width" Value="22"></Setter>
        <Setter Property="Template" >
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Ellipse 
                        HorizontalAlignment="Center"
                        Width="{TemplateBinding Width}"       
                        Height="{TemplateBinding Height}"
                    >
                        <Ellipse.Fill >

                            <VisualBrush  >
                                <VisualBrush.Visual >
                                    <Grid Background="Transparent" >
                                        <Image 

                                            Stretch="UniformToFill"
                                            Source="../../Images/CancelButton.png"></Image>
                                    </Grid>
                                </VisualBrush.Visual>
                            </VisualBrush>
                        </Ellipse.Fill>
                    </Ellipse>

                </ControlTemplate>
            </Setter.Value>
            
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Height" Value="30"></Setter>
                <Setter Property="Width" Value="25"></Setter>
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>