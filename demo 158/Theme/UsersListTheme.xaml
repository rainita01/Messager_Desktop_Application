<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:demo_158.Services"
                    >

    <Style x:Key="ContactsListTheme" TargetType="{x:Type ListView}">
        <Setter Property="Background" Value="Transparent"></Setter>
        <Setter Property="BorderThickness" Value="0"></Setter>
    </Style>

    <Style x:Key="ContactsListItemTheme" TargetType="{x:Type ListViewItem}"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation">
        <Setter Property="Cursor" Value="Hand" ></Setter>
        <Setter Property="ContextMenu">
            <Setter.Value>
                <ContextMenu>
                    <MenuItem Header="Profile 👤" Command="{Binding ShowUserContentCommand}"></MenuItem>
                    <MenuItem Header="Delete 🗑" Command="{Binding DeleteConversation}"></MenuItem>
                </ContextMenu>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
 
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListViewItem}">
                  
                        <Border 
                            Padding="3"
                            BorderThickness="0"
                            MaxWidth="181"
                            CornerRadius="3"
                            Background="{TemplateBinding Background}" >
                            
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition></ColumnDefinition>
                                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                                </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal">
                            <Grid Width="40" Height="40">
                                <Ellipse Fill="White"/>
                                <Ellipse>
                                    <Ellipse.Fill>
                                        <ImageBrush
                                            ImageSource="{Binding ContactUserModel.ContactImage}" 
                                                   
                                            Stretch="UniformToFill"/>
                                    </Ellipse.Fill>
                                </Ellipse>
                            </Grid>
                            <StackPanel >
                                <TextBlock
                                    Margin="10 0 0 0"
                                    Text="{Binding ContactUserModel.ContactUsername}"
                                    VerticalAlignment="Top"
                                    FontSize="18"></TextBlock>
                                <TextBlock 
                                    Opacity="20"
                                    Margin="10 10 0 0 " 
                                    Text="{Binding LastMessage.Text}"
                                    FontSize="12" FontFamily="Arial" Foreground="Gray">

                                </TextBlock>
                            </StackPanel>
                       
                        </StackPanel>
                                <Border
                                    Grid.Column="1"
                                    HorizontalAlignment="Right"
                                    CornerRadius="10 10 10 10"
                                    BorderThickness="2"
                                    Background="DarkMagenta"
                                    Name="NewLengthBorder"
                                    Height="28"
                                    Width="27"
                                    Visibility="{Binding NewMessageVisibility}"
                                >
                                    <TextBlock
                                        Foreground="GhostWhite"
                                        Text="{Binding NewMessagesLength}"
                                        FontSize="14"
                                        Padding="1"
                                        FontWeight="DemiBold"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                    >

                                    </TextBlock>
                                </Border>
                        </Grid>
                    </Border>
                 
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Background" Value="Transparent"></Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="WhiteSmoke"></Setter>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="LightBlue"></Setter>
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style TargetType="Thumb">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}" >
                    <Rectangle Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" Fill="Gray"></Rectangle>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>